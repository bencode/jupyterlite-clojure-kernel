{
  "name": "jupyterlite-clojure-kernel-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:install": "jupyter labextension develop . --overwrite",
    "dev:build": "jupyter lite build"
  }
}
